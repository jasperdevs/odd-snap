# OddSnap v0.8.47

## Fixed

- Keep OddSnap windows visible during captures when Windows native capture exclusion is available, eliminating capture-time UI flashes.
- Remove toast fade and shadow flicker by keeping the native window stationary and closing it immediately after the animation.
