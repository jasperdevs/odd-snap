# OddSnap v0.8.48

## Added

- Add configurable tray left-click actions and recording-window detection. (#43, #60)
- Add `{ms}` and `{process}` filename-template tokens. (#57)
- Add a setting for creating a Start menu shortcut. (#58)
- Add live region coordinates and a hotkey for capturing the last selected region. (#59)
- Allow modifierless F1-F24 hotkeys after explicit confirmation. (#71)

## Fixed

- Add clear Enter, Escape, and Space controls and save status to scrolling capture. (#65)
- Keep pinned screenshot toasts stacked with working actions and overflow handling. (#72)
